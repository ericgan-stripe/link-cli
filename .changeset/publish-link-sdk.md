---
'@stripe/link-sdk': patch
---

Publish the Link SDK with a credential-injection API for authenticated Link
requests. Applications provide either a fixed access token or an access-token
provider; login, refresh-token storage, and authorization state stay with the
host application.
