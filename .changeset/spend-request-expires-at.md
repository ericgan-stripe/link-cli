---
"@stripe/link-cli": minor
---

Add an undocumented `--expires-at` option to `spend-request create` for allow-listed OAuth clients, allowing spend request expiration to be extended up to 7 days out instead of the default 12 hours.
